function fizzbuzz(n: int)
{
    for i = 1 to n
    {
        if (i % 15 == 0)
            printLn("fizzbuzz")
        else if (i % 3 == 0)
            printLn("fizz")
        else if (i % 5 == 0)
            printLn("buzz")
        else
            printLn(string(i))
    }
}